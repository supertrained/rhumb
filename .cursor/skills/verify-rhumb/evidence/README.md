# Verification evidence

Helpers write one directory per `VERIFY_RHUMB_RUN_ID` here. Cleanup removes `/tmp/verify-rhumb-state-*` only. It never deletes this tree.

The generator proof for this skill is `20260910T014200Z` (Index search on `https://api.rhumb.dev`).
