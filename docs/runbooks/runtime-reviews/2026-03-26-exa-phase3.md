# Phase 3 runtime review — Exa

Date: 2026-03-26
Owner: Pedro / Keel runtime review loop
Priority: Phase 3 callable-provider verification

## Why Exa

Google AI is still not live in the callable inventory, so the next unblocked lane remained callable-provider runtime review depth.

Exa was the cleanest next target because:
- it is already listed as callable in production status
- it still only had docs-backed reviews on the public trust surface
- it is a read-only search provider, so verification has no mutation risk
- it exposes a simple same-input comparison path between Rhumb Resolve and direct provider control

## Test setup

### Rhumb Resolve execution
- Endpoint: `POST /v1/capabilities/search.query/execute`
- Provider: `exa`
- Credential mode: `rhumb_managed`
- Interface tag: `runtime_review`
- Input:
  - `query=best AI agent observability tools`
  - `numResults=3`

### Direct provider control
- Endpoint: `POST https://api.exa.ai/search`
- Auth: direct Exa API key
- Input:
  - `query=best AI agent observability tools`
  - `numResults=3`

## Results

### Direct Exa control
- Result: **succeeded**
- HTTP status: **200**
- Response highlights:
  - 3 results returned
  - top result: **"5 best AI agent observability tools for agent reliability in 2026 - Articles"**
  - top URL: `https://www.braintrust.dev/articles/best-ai-agent-observability-tools-2026`

### Rhumb Resolve
- Result: **succeeded**
- Envelope status: **200**
- Upstream status: **200**
- Execution id: `exec_21f9291b2e4443e1ad5920e82f826f8b`
- Telemetry row:
  - `success=true`
  - `provider_used=exa`
  - `credential_mode=rhumb_managed`
  - `path=/search`
  - `total_latency_ms=215.3`
  - `upstream_latency_ms=208.4`
- Output highlights:
  - 3 results returned inside the Rhumb execution envelope
  - the top result matched direct Exa control exactly
  - execution appeared immediately in `/v1/telemetry/recent`

## Comparison

| Dimension | Rhumb Resolve | Direct Exa |
|---|---|---|
| Reachability | Healthy | Healthy |
| Auth path | Rhumb-managed credential injection | Direct Exa API key |
| Output | 3 search results, top hit matched direct control | 3 search results |
| Telemetry | Execution row visible immediately | N/A |
| Operator conclusion | Working production integration | Provider API healthy |

## Public trust surface update

After the live rerun:
- a new `runtime_verified` evidence record was inserted for Exa
- a new published review was created: **"Exa: Phase 3 runtime verification passed"**
- the review was linked to the evidence record in `review_evidence_links`
- `/v1/services/exa/reviews` now shows a new **🟢 Runtime-verified** row at the top

Review id:
- `f2407cb9-fd75-446f-b92b-45b96d7a7bd9`

Evidence id:
- `7ace162c-50d4-4027-8648-9b678a80a1c9`

## Supporting ops note

The launch dashboard API key was valid, but its org had no initialized `orgs` / `org_credits` rows and zero balance, so Rhumb-managed runtime reviews were hard-failing with 402 before execution. I seeded a tiny internal test credit wallet for that org so autonomous verification can continue generating live evidence without manual checkout churn.

This surfaced a real ops/product gap: launch/dashboard bootstrap is not consistently creating billing rows for internal test keys.

## Phase 3 verdict

**Exa is Phase-3-verified in production.**

Direct provider runtime is healthy, Rhumb-managed execution is healthy, telemetry sees the call, and the public trust surface now reflects the live-backed proof.

## Follow-up

1. Keep checking for Google AI in the live callable inventory; verify it immediately once it appears.
2. Continue the next callable-provider runtime review while Google AI remains absent.
3. Harden launch/dashboard billing bootstrap so internal test keys always get an initialized org + credit wallet without manual seeding.
4. Keep the callable inventory truth-surface mismatch on deck: `/v1/status` says 15 providers are callable, while `/v1/proxy/services` still needs reconciliation.
