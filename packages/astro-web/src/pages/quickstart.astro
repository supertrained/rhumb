---
export const prerender = true;
import Layout from "../layouts/Layout.astro";
import ActivationPathCards from "../components/ActivationPathCards.astro";
import { PRIMARY_ACTIVATION_PATHS } from "../lib/activation-paths";
import { PUBLIC_TRUTH } from "../lib/public-truth";

const API = "https://api.rhumb.dev/v1";
const QUICKSTART_JSON_LD = {
  "@context": "https://schema.org",
  "@type": "WebPage",
  name: "Quickstart — Rhumb",
  url: "https://rhumb.dev/quickstart",
  description: "Make your first Rhumb API call in 30 seconds, or resolve and execute a managed web-search capability in three scoped calls.",
};

const DISCOVERY_TRACKS = [
  {
    kicker: "Service discovery",
    title: "Use services when you are choosing who to trust",
    detail:
      "Service search answers the vendor question. It returns scored services, not executable action IDs.",
    href: "/leaderboard",
    cta: "Browse services",
    commands: [
      `curl "${API}/search?q=web+search"`,
      `curl "${API}/leaderboard/research"`,
      `curl "${API}/services/brave-search"`,
    ],
  },
  {
    kicker: "Capability discovery",
    title: "Use capabilities when you know the job but not the slug",
    detail:
      "Capability search answers the action question. It returns IDs like search.query and email.send, then points you into resolve and estimate.",
    href: "/capabilities",
    cta: "Browse capabilities",
    commands: [
      `curl "${API}/capabilities?search=web+research"`,
      `curl "${API}/capabilities/search.query/resolve"`,
      `curl "${API}/capabilities/search.query/execute/estimate"`,
    ],
  },
];


const WEB_SEARCH_QUICK_PATH = [
  {
    label: "1. Resolve the capability",
    detail: "Free read. Confirms whether `search.query` has a current managed route before you write code around it.",
    command: `curl "${API}/capabilities/search.query/resolve"`,
  },
  {
    label: "2. Estimate the execution rail",
    detail: "Free preflight. Checks the concrete provider, credential lane, and cost envelope before any paid call.",
    command: `curl "${API}/capabilities/search.query/execute/estimate"`,
  },
  {
    label: "3. Execute with a governed key",
    detail: "Authorized execution. This is the side-effecting step, so keep it behind `X-Rhumb-Key` or an equivalent payment rail.",
    command: `curl -X POST "${API}/capabilities/search.query/execute" \
  -H "X-Rhumb-Key: rhumb_live_..." \
  -H "Content-Type: application/json" \
  -d '{"body": {"query": "best CRM for seed-stage B2B SaaS", "max_results": 5}}'`,
  },
];

const agentContext =
  `Rhumb quickstart. Start with free discovery endpoints for services and capabilities, then move to Resolve only when the capability is supported. Read endpoints do not require signup; governed execution requires an appropriate credential or payment path.`;
---

<Layout
  title="Quickstart — Rhumb"
  description="Make your first Rhumb API call in 30 seconds, or resolve and execute a managed web-search capability in three scoped calls."
  canonical="https://rhumb.dev/quickstart"
  ogTitle="Rhumb Quickstart"
  ogDescription="Zero to first API response in 30 seconds, plus a scoped web-search Resolve path: resolve, estimate, execute."
  ogUrl="https://rhumb.dev/quickstart"
  jsonLd={QUICKSTART_JSON_LD}

  agentContext={agentContext}>
  <div class="bg-navy min-h-screen">
    <div class="max-w-4xl mx-auto px-6 py-16">
      <!-- Header -->
      <div class="mb-12">
        <p class="text-xs font-mono text-amber uppercase tracking-widest mb-2">Quickstart</p>
        <h1 class="font-display font-bold text-4xl sm:text-5xl text-slate-100 leading-tight tracking-tight">
          First call in 30 seconds
        </h1>
        <p class="mt-4 text-lg text-slate-400 leading-relaxed max-w-2xl">
          No signup required. Copy a command, get a response. Read endpoints are free and open.
        </p>
      </div>

      <section class="mb-16">
        <div class="bg-surface border border-slate-800 rounded-2xl p-6 sm:p-8">
          <p class="text-xs font-mono text-amber uppercase tracking-widest mb-2">Before you wire this deeper</p>
          <p class="text-sm text-slate-300 leading-6 max-w-3xl">
            Want to evaluate the trust story first? Read <a href="/trust" class="text-amber hover:underline underline-offset-2">Trust</a>,
            inspect the public <a href="/methodology" class="text-amber hover:underline underline-offset-2">Methodology</a>,
            or use the provider <a href="/providers#dispute-a-score" class="text-amber hover:underline underline-offset-2">dispute path</a>
            if a score or claim looks wrong.
          </p>
        </div>
      </section>

      <ActivationPathCards
        eyebrow="Route picker"
        title="Pick the path that matches why you are here"
        description="This page is the curl-first path, but cold-start builders and operators should not have to infer that. Evaluators start on free reads. Agent builders start with MCP. Teams that want governed repeat execution start with account setup and X-Rhumb-Key."
        routes={PRIMARY_ACTIVATION_PATHS}
      />

      <section class="mb-16">
        <div class="grid gap-4 md:grid-cols-2">
          <div class="bg-surface border border-emerald-500/20 rounded-2xl p-6">
            <p class="text-xs font-mono text-emerald-300 uppercase tracking-widest mb-2">Best first win</p>
            <p class="text-sm text-slate-300 leading-6">
              Rhumb is strongest today for {PUBLIC_TRUTH.beachheadLabel}. If you want a clean first success, start there.
            </p>
          </div>
          <div class="bg-surface border border-amber/20 rounded-2xl p-6">
            <p class="text-xs font-mono text-amber uppercase tracking-widest mb-2">Don’t overread the promise</p>
            <p class="text-sm text-slate-300 leading-6">
              If you are wiring multi-system back-office workflows, use Rhumb as capability infrastructure first. Don’t assume the current product is already a general business-automation layer.
            </p>
          </div>
        </div>
      </section>


      <section class="mb-16 scroll-mt-28" id="web-search-resolve">
        <div class="bg-surface border border-emerald-500/20 rounded-2xl p-6 sm:p-8">
          <div class="max-w-3xl mb-6">
            <p class="text-xs font-mono text-emerald-300 uppercase tracking-widest mb-2">Web-search Resolve path</p>
            <h2 class="font-display font-bold text-2xl text-slate-100">Resolve a web-search capability in three calls</h2>
            <p class="text-slate-400 text-sm mt-2 leading-relaxed">
              If you came here for the `search.query` quickstart, use this bounded path: resolve first, estimate second, execute only after you have a governed key or payment rail. The first two calls are read/preflight checks; the third is the paid or authorized execution step.
            </p>
            <p class="text-slate-500 text-xs mt-3 leading-5">
              Still choosing the provider? Read <a href="/blog/choose-web-search-api-for-ai-agents-route-safely" class="text-amber hover:underline underline-offset-2">how to choose a web search API for agents and route the call safely</a> before pinning a vendor SDK in an unattended loop.
            </p>
          </div>
          <div class="grid gap-4 lg:grid-cols-3">
            {WEB_SEARCH_QUICK_PATH.map((step) => (
              <div class="rounded-xl border border-slate-800 bg-navy p-5">
                <p class="text-xs font-mono text-emerald-300 uppercase tracking-wider mb-2">{step.label}</p>
                <p class="text-sm text-slate-400 leading-6 mb-4">{step.detail}</p>
                <pre class="rounded-lg border border-slate-800 bg-surface px-4 py-3 text-xs font-mono text-slate-300 overflow-x-auto"><code>{step.command}</code></pre>
              </div>
            ))}
          </div>
        </div>
      </section>

      <section class="mb-16">
        <div class="bg-surface border border-slate-800 rounded-2xl p-6 sm:p-8">
          <div class="max-w-3xl mb-6">
            <p class="text-xs font-mono text-amber uppercase tracking-widest mb-2">Mental model</p>
            <h2 class="font-display font-bold text-2xl text-slate-100">Use one default path unless you have a reason not to</h2>
            <p class="text-slate-400 text-sm mt-2 leading-relaxed">
              {PUBLIC_TRUTH.resolveMentalModel.defaultPath}
            </p>
          </div>

          <div class="grid gap-4 md:grid-cols-2 xl:grid-cols-4 mb-4">
            <div class="rounded-xl border border-slate-800 bg-navy p-5">
              <p class="text-xs font-mono text-slate-500 uppercase tracking-wider mb-2">Think in entities</p>
              <p class="text-sm text-slate-300 leading-6">
                <strong class="text-slate-100">Service</strong> = vendor. <strong class="text-slate-100">Capability</strong> = executable action. <strong class="text-slate-100">Recipe</strong> = compiled workflow.
              </p>
            </div>
            <div class="rounded-xl border border-slate-800 bg-navy p-5">
              <p class="text-xs font-mono text-slate-500 uppercase tracking-wider mb-2">Default layer</p>
              <p class="text-sm text-slate-300 leading-6">
                Start with <strong class="text-slate-100">Layer 2</strong>: capability routing. It is the cleanest real production surface today.
              </p>
            </div>
            <div class="rounded-xl border border-slate-800 bg-navy p-5">
              <p class="text-xs font-mono text-slate-500 uppercase tracking-wider mb-2">Default auth rail</p>
              <p class="text-sm text-slate-300 leading-6">
                Use governed API key or wallet-prefund on <code class="text-amber">X-Rhumb-Key</code> for repeat calls. Bring BYOK or Agent Vault only when provider control is the point. Use x402 only when zero-signup per-call payment is the point.
              </p>
            </div>
            <div class="rounded-xl border border-slate-800 bg-navy p-5">
              <p class="text-xs font-mono text-slate-500 uppercase tracking-wider mb-2">Canonical docs</p>
              <p class="text-sm text-slate-300 leading-6">
                Full map: <a href="/docs#resolve-mental-model" class="text-amber hover:underline">/docs#resolve-mental-model</a>
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- Step 1: Try the API -->
      <section class="mb-16 scroll-mt-28" id="rest-path">
        <div class="flex items-center gap-3 mb-6">
          <span class="flex items-center justify-center w-8 h-8 rounded-full bg-amber/10 text-amber font-mono font-bold text-sm">1</span>
          <h2 class="font-display font-bold text-2xl text-slate-100">Try the API</h2>
        </div>
        
        <p class="text-slate-400 mb-6">All read endpoints are free, no key required. Pick your language:</p>

        <!-- curl -->
        <div class="mb-6">
          <p class="text-xs font-mono text-slate-500 uppercase tracking-widest mb-2">curl</p>
          <pre class="bg-surface border border-slate-800 rounded-lg p-4 overflow-x-auto text-sm font-mono text-slate-300"><code>curl "{API}/services/stripe/score"</code></pre>
        </div>

        <!-- JavaScript -->
        <div class="mb-6">
          <p class="text-xs font-mono text-slate-500 uppercase tracking-widest mb-2">JavaScript</p>
          <pre class="bg-surface border border-slate-800 rounded-lg p-4 overflow-x-auto text-sm font-mono text-slate-300"><code>{`const res = await fetch("${API}/services/stripe/score");
const { data } = await res.json();
console.log(data.an_score); // 8.1`}</code></pre>
        </div>

        <!-- Python -->
        <div class="mb-6">
          <p class="text-xs font-mono text-slate-500 uppercase tracking-widest mb-2">Python</p>
          <pre class="bg-surface border border-slate-800 rounded-lg p-4 overflow-x-auto text-sm font-mono text-slate-300"><code>{`import requests
data = requests.get("${API}/services/stripe/score").json()["data"]
print(data["an_score"])  # 8.1`}</code></pre>
        </div>

        <div class="bg-surface border border-slate-800 rounded-lg p-4 text-sm text-slate-400">
          <strong class="text-slate-200">Response (abbreviated):</strong>
          <pre class="mt-2 text-xs font-mono overflow-x-auto"><code>{`{
  "data": {
    "slug": "stripe",
    "an_score": 8.1,
    "tier": "L4 Native",
    "execution_score": 8.4,
    "access_readiness_score": 7.3,
    "failure_count": 2,
    "review_count": 47
  }
}`}</code></pre>
        </div>
      </section>

      <!-- Step 2: Explore -->
      <section class="mb-16">
        <div class="flex items-center gap-3 mb-6">
          <span class="flex items-center justify-center w-8 h-8 rounded-full bg-amber/10 text-amber font-mono font-bold text-sm">2</span>
          <h2 class="font-display font-bold text-2xl text-slate-100">Explore by vendor or by job</h2>
        </div>

        <p class="text-slate-400 mb-6 max-w-3xl leading-relaxed">
          Do not force one endpoint to answer two different questions. <code class="bg-elevated px-1 py-0.5 rounded text-amber text-xs">/search</code>
          tells you which services look best. <code class="bg-elevated px-1 py-0.5 rounded text-amber text-xs">/capabilities?search=</code> tells you which executable action IDs Rhumb already understands.
        </p>

        <div class="grid gap-4 lg:grid-cols-2 mb-6">
          {DISCOVERY_TRACKS.map((track) => (
            <div class="bg-surface border border-slate-800 rounded-2xl p-6">
              <div class="flex items-start justify-between gap-4 mb-3">
                <div>
                  <p class="text-xs font-mono text-amber uppercase tracking-widest mb-2">{track.kicker}</p>
                  <h3 class="font-display font-semibold text-xl text-slate-100">{track.title}</h3>
                </div>
                <a href={track.href} class="text-xs font-mono text-slate-400 hover:text-amber transition-colors whitespace-nowrap">{track.cta} →</a>
              </div>
              <p class="text-sm text-slate-400 leading-6">{track.detail}</p>
              <div class="mt-4 space-y-3">
                {track.commands.map((command) => (
                  <pre class="rounded-xl border border-slate-800 bg-navy px-4 py-3 text-xs font-mono text-slate-300 overflow-x-auto"><code>{command}</code></pre>
                ))}
              </div>
            </div>
          ))}
        </div>

        <div class="grid gap-4 md:grid-cols-2">
          <div class="bg-surface border border-amber/20 rounded-xl p-5">
            <p class="text-xs font-mono text-amber uppercase tracking-widest mb-2">Mental shortcut</p>
            <p class="text-sm text-slate-300 leading-6">
              <strong class="text-slate-100">Service</strong> = vendor to evaluate. <strong class="text-slate-100">Capability</strong> = action to execute. If the question is “who should I use?”, search services. If the question is “what exact action slug do I call?”, search capabilities.
            </p>
          </div>
          <div class="bg-surface border border-emerald-500/20 rounded-xl p-5">
            <p class="text-xs font-mono text-emerald-300 uppercase tracking-widest mb-2">Resolve then estimate</p>
            <p class="text-sm text-slate-300 leading-6">
              Read <strong class="text-slate-100">resolve</strong> as the ranked recommendation. Read <strong class="text-slate-100">estimate</strong> as the concrete provider on the rail you are actually about to use. They answer different questions on purpose.
            </p>
          </div>
        </div>
      </section>

      <!-- Step 3: Execute capabilities -->
      <section class="mb-16 scroll-mt-28" id="execute-path">
        <div class="flex items-center gap-3 mb-6">
          <span class="flex items-center justify-center w-8 h-8 rounded-full bg-amber/10 text-amber font-mono font-bold text-sm">3</span>
          <h2 class="font-display font-bold text-2xl text-slate-100">Execute through governed API key first</h2>
        </div>

        <p class="text-slate-400 mb-6">
          For the cleanest first paid success, start with a Rhumb-managed capability behind <code class="bg-elevated px-1 py-0.5 rounded text-amber text-xs">X-Rhumb-Key</code>. Sign up creates identity. Credits or wallet-prefund unlock governed execution. Prove the managed Layer 2 path first, then bring BYOK or Agent Vault only when the workflow crosses into your CRM, support, data, or ops systems.
        </p>

        <div class="grid gap-4 xl:grid-cols-[1.15fr_0.85fr] mb-6">
          <div class="bg-surface border border-slate-800 rounded-lg p-5">
            <p class="text-xs font-mono text-emerald-300 uppercase tracking-widest mb-2">Lowest-heroics first win</p>
            <pre class="text-sm font-mono text-slate-300 overflow-x-auto"><code>{`# 1) Resolve a managed capability
curl "${API}/capabilities/search.query/resolve"

# 2) Estimate before paying
curl "${API}/capabilities/search.query/execute/estimate"

# 3) Execute through Layer 2 with your governed API key
curl -X POST "${API}/capabilities/search.query/execute" \
  -H "X-Rhumb-Key: rhumb_live_..." \
  -H "Content-Type: application/json" \
  -d '{"body": {"query": "best CRM for seed-stage B2B SaaS", "max_results": 5}}'`}</code></pre>
          </div>
          <div class="bg-surface border border-amber/20 rounded-lg p-5">
            <p class="text-xs font-mono text-amber uppercase tracking-widest mb-2">Bring bridges later</p>
            <p class="text-sm text-slate-300 leading-6">
              Keep the same Resolve flow when you move into Salesforce, HubSpot, Zendesk, Postgres, GitHub, or other operator-owned systems. The change is the credential path, not the product mental model.
            </p>
          </div>
        </div>

        <div class="bg-surface border border-slate-800 rounded-lg p-5 mb-6">
          <p class="text-xs font-mono text-slate-500 uppercase tracking-widest mb-2">Same flow, different capability</p>
          <pre class="text-sm font-mono text-slate-300 overflow-x-auto"><code>{`# 1) Discover the capability you need
curl "${API}/capabilities?search=email"

# 2) Resolve ranked providers, or follow the machine-readable recovery handoff
curl "${API}/capabilities/email.send/resolve?credential_mode=byok"

# 3) Estimate before paying
curl "${API}/capabilities/email.send/execute/estimate"

# 4) Execute through Layer 2 with X-Rhumb-Key
curl -X POST "${API}/capabilities/email.send/execute" \
  -H "X-Rhumb-Key: rhumb_live_..." \
  -H "Content-Type: application/json" \
  -d '{"body": {"to": "user@example.com", "subject": "Hello", "body": "Hi from Rhumb"}}'`}</code></pre>
          <p class="text-sm text-slate-400 mt-4 leading-6">
            If the requested rail dead-ends, Resolve returns machine-readable recovery fields like <code class="bg-elevated px-1 py-0.5 rounded text-amber text-xs">recovery_hint.resolve_url</code>, <code class="bg-elevated px-1 py-0.5 rounded text-amber text-xs">recovery_hint.credential_modes_url</code>, and, when applicable, <code class="bg-elevated px-1 py-0.5 rounded text-amber text-xs">recovery_hint.alternate_execute_hint</code> or <code class="bg-elevated px-1 py-0.5 rounded text-amber text-xs">recovery_hint.setup_handoff</code> instead of forcing another discovery pass. If the capability ID is wrong, you get search suggestions rather than a blank dead end.
          </p>
        </div>

        <div class="grid sm:grid-cols-4 gap-4 mb-8">
          <div class="bg-surface border border-slate-800 rounded-lg p-5">
            <h3 class="font-display font-semibold text-slate-100 mb-2">Governed API key</h3>
            <p class="text-xs text-slate-400 mb-3">Sign up, add credits, then pass the key as <code class="bg-elevated px-1 py-0.5 rounded text-amber text-xs">X-Rhumb-Key</code>.</p>
            <span class="text-xs font-mono text-slate-500">Best for: default production path</span>
          </div>
          <div class="bg-surface border border-amber/20 rounded-lg p-5">
            <h3 class="font-display font-semibold text-slate-100 mb-2">x402 Payment</h3>
            <p class="text-xs text-slate-400 mb-3">Zero-signup, per-call USDC payment when request-level payment authorization is the point.</p>
            <span class="text-xs font-mono text-amber">Best for: zero-signup per-call auth</span>
          </div>
          <div class="bg-surface border border-emerald-500/20 rounded-lg p-5" id="wallet-prefund">
            <h3 class="font-display font-semibold text-slate-100 mb-2">Wallet Prefund</h3>
            <p class="text-xs text-slate-400 mb-3">Wallet-auth once, top up reusable balance, execute with <code class="text-emerald-300">X-Rhumb-Key</code>.</p>
            <span class="text-xs font-mono text-emerald-300">Best for: repeat wallet traffic</span>
          </div>
          <div class="bg-surface border border-slate-800 rounded-lg p-5">
            <h3 class="font-display font-semibold text-slate-100 mb-2">BYOK / Agent Vault</h3>
            <p class="text-xs text-slate-400 mb-3">Use BYOK for direct pass-through, or Agent Vault for encrypted provider credential injection when provider control is the point.</p>
            <span class="text-xs font-mono text-slate-500">Best for: teams, enterprise</span>
          </div>
        </div>

        <div class="bg-surface border border-slate-800 rounded-lg p-5 mb-4">
          <p class="text-xs font-mono text-slate-500 uppercase tracking-widest mb-2">Execute via governed API key</p>
          <pre class="text-sm font-mono text-slate-300 overflow-x-auto"><code>{`curl -X POST "${API}/capabilities/email.send/execute" \\
  -H "X-Rhumb-Key: rhumb_live_..." \\
  -H "Content-Type: application/json" \\
  -d '{"body": {"to": "user@example.com", "subject": "Hello", "body": "Hi from Rhumb"}}'`}</code></pre>
        </div>

        <div class="bg-surface border border-emerald-500/20 rounded-lg p-5 mb-4">
          <p class="text-xs font-mono text-emerald-300 uppercase tracking-widest mb-2">Execute via wallet-prefund</p>
          <pre class="text-sm font-mono text-slate-300 overflow-x-auto"><code>{`# Step 1: Prove wallet control and create wallet session
curl -X POST "${API}/auth/wallet/request-challenge" \
  -H "Content-Type: application/json" \
  -d '{"chain":"base","address":"0x...","purpose":"access"}'

curl -X POST "${API}/auth/wallet/verify" \
  -H "Content-Type: application/json" \
  -d '{"challenge_id":"...","signature":"0x..."}'
# Returns wallet session + wallet-scoped X-Rhumb-Key credential

# Step 2: Top up reusable balance via x402 settlement
curl -X POST "${API}/billing/x402/topup/request" \
  -H "Authorization: Bearer <wallet_session_token>" \
  -H "Content-Type: application/json" \
  -d '{"amount_usd": 1.00}'
# ...settle + verify through /billing/x402/topup/verify...

# Step 3: Execute repeat calls with X-Rhumb-Key (no per-call X-Payment)
curl -X POST "${API}/capabilities/email.send/execute" \
  -H "X-Rhumb-Key: rhumb_live_..." \
  -H "Content-Type: application/json" \
  -d '{"body": {"to": "user@example.com", "subject": "Hello"}}'`}</code></pre>
        </div>

        <div class="bg-surface border border-amber/20 rounded-lg p-5">
          <p class="text-xs font-mono text-amber uppercase tracking-widest mb-2">Execute via x402 (zero signup)</p>
          <pre class="text-sm font-mono text-slate-300 overflow-x-auto"><code>{`# Step 1: Request execution without a governed API key
curl -X POST "${API}/capabilities/email.send/execute" \\
  -H "Content-Type: application/json" \\
  -d '{"body": {"to": "user@example.com", "subject": "Hello"}}'
# Returns 402 with the exact asset, amount, address, and network to pay

# Step 2: Pay the exact requirement from your wallet
# Double-check asset + network + destination address before sending funds

# Step 3: Retry with payment proof from the same wallet
curl -X POST "${API}/capabilities/email.send/execute" \\
  -H 'X-Payment: {"tx_hash":"0x...","network":"<network-from-402>","wallet_address":"0x..."}' \\
  -H "Content-Type: application/json" \\
  -d '{"body": {"to": "user@example.com", "subject": "Hello"}}'`}</code></pre>
          <div class="mt-4 rounded-xl border border-amber/30 bg-amber/5 p-4 text-sm leading-6 text-slate-300">
            <p class="text-xs font-mono uppercase tracking-widest text-amber mb-2">Honest note</p>
            <p>
              The repeatable public x402 execute path today is tx-hash proof. Wallet-prefund top-up verification is the
              standard x402 authorization path; direct execute is not. Some smart-wallet or wrapped authorization
              payloads still hit a production compatibility boundary on the direct execute rail because the facilitator path is not wired.
              If your buyer emits wrapped proofs, switch to <a href="#wallet-prefund" class="text-amber hover:underline underline-offset-2">wallet-prefund</a>
              or use the standard governed API key rail.
            </p>
          </div>
        </div>
      </section>

      <!-- Step 4: MCP -->
      <section class="mb-16 scroll-mt-28" id="mcp-path">
        <div class="flex items-center gap-3 mb-6">
          <span class="flex items-center justify-center w-8 h-8 rounded-full bg-amber/10 text-amber font-mono font-bold text-sm">4</span>
          <h2 class="font-display font-bold text-2xl text-slate-100">Connect via MCP</h2>
        </div>

        <p class="text-slate-400 mb-6">
          If your agent supports <a href="https://spec.modelcontextprotocol.io/" class="text-amber hover:underline">MCP</a>, connect directly:
        </p>

        <div class="bg-surface border border-slate-800 rounded-lg p-5 mb-6">
          <p class="text-xs font-mono text-slate-500 uppercase tracking-widest mb-2">Install</p>
          <pre class="text-sm font-mono text-slate-300"><code>npx rhumb-mcp@latest</code></pre>
        </div>

        <div class="bg-surface border border-slate-800 rounded-lg p-5 mb-6">
          <p class="text-xs font-mono text-slate-500 uppercase tracking-widest mb-2">Claude Desktop config</p>
          <pre class="text-sm font-mono text-slate-300 overflow-x-auto"><code>{`{
  "mcpServers": {
    "rhumb": {
      "command": "npx",
      "args": ["rhumb-mcp@latest"],
      "env": {
        "RHUMB_API_KEY": "rhumb_live_..."
      }
    }
  }
}`}</code></pre>
        </div>

        <p class="text-slate-400 text-sm">
          {PUBLIC_TRUTH.mcpToolsLabel} tools available: <code class="bg-elevated px-1 py-0.5 rounded text-amber text-xs">find_services</code>, 
          <code class="bg-elevated px-1 py-0.5 rounded text-amber text-xs">get_score</code>, 
          <code class="bg-elevated px-1 py-0.5 rounded text-amber text-xs">get_alternatives</code>, 
          <code class="bg-elevated px-1 py-0.5 rounded text-amber text-xs">get_failure_modes</code>, 
          <code class="bg-elevated px-1 py-0.5 rounded text-amber text-xs">execute_capability</code>, and more.
          <a href="/blog/getting-started-mcp" class="text-amber hover:underline ml-1">Full MCP guide →</a>
        </p>
      </section>

      <!-- Next steps -->
      <section class="border-t border-slate-800 pt-10">
        <h2 class="font-display font-bold text-xl text-slate-100 mb-6">Keep going</h2>
        <div class="grid sm:grid-cols-3 gap-4">
          <a href="/docs" class="group block bg-surface border border-slate-800 rounded-lg p-5 hover:border-slate-600 transition-colors">
            <span class="text-sm font-medium text-slate-200 group-hover:text-amber transition-colors">API Reference</span>
            <span class="block text-xs text-slate-500 mt-1">All endpoints, parameters, responses</span>
          </a>
          <a href="/blog/securing-keys-for-agents" class="group block bg-surface border border-slate-800 rounded-lg p-5 hover:border-slate-600 transition-colors">
            <span class="text-sm font-medium text-slate-200 group-hover:text-amber transition-colors">Credential Security</span>
            <span class="block text-xs text-slate-500 mt-1">BYOK, Agent Vault, governed API key, wallet-prefund, x402 — which path fits your agent</span>
          </a>
          <a href="/compare" class="group block bg-surface border border-slate-800 rounded-lg p-5 hover:border-slate-600 transition-colors">
            <span class="text-sm font-medium text-slate-200 group-hover:text-amber transition-colors">Tool Comparisons</span>
            <span class="block text-xs text-slate-500 mt-1">9 head-to-head comparisons with live data</span>
          </a>
        </div>
      </section>
    </div>
  </div>
</Layout>
